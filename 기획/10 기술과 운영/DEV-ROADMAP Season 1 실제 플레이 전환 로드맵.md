# WildSurvival Season 1 실제 플레이 전환 로드맵

## 문서 정보

| 항목 | 내용 |
|---|---|
| 문서 ID | `PRODUCTION-DEV-ROADMAP-S1-001` |
| 상태 | `ACTIVE_CONTROL` |
| 적용 범위 | 프로토타입 이후 Season 1 실제 플레이 전환 순서와 승인 게이트 |
| 선행 | `DEV-ROADMAP-001`, `PRODUCTION-COMPLETION-PLAN-001`, `IMPLEMENTATION-HANDOFF-001` |
| 통합 브랜치 | `S1_dev` |
| 재개 기준일 | 2026-08-24 |
| 실행 상태 | `P2_IMPLEMENTED_AWAITING_CLIENT_EVIDENCE` — P1 통과, P2 개인 자원·Craft 거래 구현 및 자동 검증 통과, 실클라이언트·프로세스 증거 대기 |
| 재개 기준 커밋 | `8eaa421` |
| 활성 목표 | `GOAL-S1-PRODUCTION-PLAYABLE-001` |
| P0 기획 입력 잠금 | `s1-plan-20260824-r1` |
| P0.5 목표 번들 | `ws-content-r2.1` — 기존 `ws-content-r2` 회차는 불변 |

## 1. 재개 기준선

아래는 중단 전에 완료된 기준선이며 현재 목표의 종료점이 아니다. 이후 단계는 P0부터 P9까지 앞 단계 게이트를 통과하는 순서로 계속 수행한다.

- slot 0 장비의 서버 권위 파손 전이와 호환 파손 이벤트 보장
- 등록 콘텐츠별 목록과 비저작 기획 공백 감사
- `F=짧은 패링/단기 방어` 입력 계약
- 응급 AP 자극제 `즉시 +10, 이후 5초간 초당 +3`와 기본 구조 고정대 `+10%` 권위·부분 런타임
- 연구 25개 `RCOST-*`, 시설 121개 `FCOST-*`의 중복 없는 구조화 비용 ID
- 후반 필수 재건비 240과 필수 연구 전문비 82, 75% 하한 대비 예비 42
- R03 무작위 3방향·정수값 추론 GUI, R04 방문 환경 2종 GUI, R05 방향 말뚝 배치 계약
- r2 생성기·스키마·L0 참조 검사와 자동 테스트
- 등록 콘텐츠 전수 목록과 실행 효과 원장의 존재·수량 재감사

중단 요청 직전의 재감사에서 다음 사실을 추가로 확인했다.

| 감사 대상 | 문서 권위 | 현재 투영 상태 | 중단 판정 |
|---|---|---|---|
| 스킬 64개 | `skill-effect-s1-r2`에 대상·비용·수치·상태·child·실패 정책 64행 잠금 | 생성기·schema·Java catalog가 배열을 읽지 않고 단일 `effect`로 축약 | `DATA_LOCKED_NOT_PROJECTED` |
| 증강 66개 | `augment-effect-s1-r2`에 opcode·trigger·stateScope·수치·ICD·상한/폴백 66행 잠금 | 생성기가 일부 예외 외에는 첫 태그를 opcode로 사용하며 trigger/stateScope를 내보내지 않음 | `DATA_LOCKED_NOT_PROJECTED` |
| 일반 적 행동 69개 | `enemy-action-s1-r2`에 필수 실행 필드 69행과 owner 53·child 참조 잠금 | 생성기가 적마다 `*-PRIMARY` 행동 하나만 생성하고 지원 엔티티는 34개 | `DATA_LOCKED_NOT_PROJECTED` |
| 사용자 저작 결정 | `AUTHOR-DECISION-REGISTER-001` | `REVIVAL_ITEM`, `CHAPTER_REVIEW` 방식 확정; Story 실제 작성은 플레이 가능 판정 뒤로 연기 | `POLICY_LOCKED_CONTENT_DEFERRED` |

재개 뒤 P0에서는 `REVIVAL_ITEM`, 64/66/69 명시 실행 행, D18 전용 추종체까지 기획 원장만 보정했다. 생성기·schema·catalog·runtime·test 투영은 P0 전체 그래프와 인계 게이트가 닫힐 때까지 시작하지 않으며, 사용자 소유 Gradle cache/report 변경은 손대거나 stage하지 않는다.

다음 항목은 완료로 오해하지 않는다.

- P2 Day 1~3의 실제 채집·craft 해금·조합·장착·전투·연구 수직 구간과 2~4인 무관리자 완주 증거
- R03~R05 실제 GUI·상태 머신·월드 상호작용
- 64 스킬·66 증강·69 일반 적 행동의 전체 의미 런타임
- 61 아이템·59 재료·214 장비·315 레시피의 실클라이언트 전수 통과
- Day 1~50, 시설 46개, 연구 25개, 엔티티 92개, loot 62개의 정상·거부·재접속·재시작 E2E
- Day 50 Final과 Story 원문 표현

## 2. 현재 활성 목표

### `GOAL-S1-PRODUCTION-PLAYABLE-001 Season 1 실제 플레이 완성`

> 프로토타입 판정을 종료하고, 등록된 모든 콘텐츠를 기획 원장·실행 데이터·Paper 런타임·저장/복구·실제 클라이언트 증거까지 연결해 1~4인이 관리자 명령 없이 Day 1부터 Day 50 Final까지 완주 가능한 Season 1을 만든다.

사용자 결정 입력은 2026-08-24에 다음과 같이 잠겼다.

1. 완전 사망 뒤 부활: `REVIVAL_ITEM`
2. Story 본문 작성 방식: `CHAPTER_REVIEW`; 실제 작성은 전체 플레이 가능 판정 이후 가장 마지막 작업

첫 단계 P0의 작업 범위는 다음과 같다.

1. `REVIVAL_ITEM`의 고정 ID·Day·비용·소유·사용·실패·복구 계약을 아이템 62·레시피 316·도감 335의 권위 목록에 반영한다.
2. 스킬 64개마다 operation 배열뿐 아니라 operation별 대상·수치·상태·자식 엔티티·실패·소비 파라미터를 명시한다.
3. 증강 66개마다 opcode·trigger·stateScope와 함께 수치·ICD·상한·중첩·파생 이벤트 차단 파라미터를 명시한다.
4. 일반 적 행동 69개마다 전조·startup/active/recovery·쿨다운·사거리·대상·피해·관통·브레이크·상태·opcode·자식 엔티티·태그·대응 태그를 한 행으로 폐쇄한다.
5. 재료 59·비장비 아이템 62·장비 214·레시피 316·스킬 64·증강 66·상태 21·일반 적 행동 69·엔티티 92·loot 62·시설 46·연구 25·Day/사건의 ID와 획득→소비→Final 그래프를 재검사한다. Story는 ID·트리거·개발 폴백 연결만 검사하고 본문은 작성하지 않는다.
6. `DATA-REVISION-002`, 구현 인계 계약, schema 필드 계약, L0~L5 증거 행렬을 갱신하고 코드 착수용 입력 리비전을 동결한다.

완료 조건은 자연어·ID 접두사·첫 태그 추론 0, Story 본문을 제외한 필수 실행 필드 공백 0, 고아 참조·의도하지 않은 순환·75% 수급 소프트락 0, 두 사용자 ADR 잠금, 실제 클라이언트 E2E 조합과 신규 회차 리비전 승인이다.

P0은 기획 완결만 수행한다. P0의 증거가 모두 닫히면 P0.5 실행 데이터 투영으로 진행하고, 이후 P1~P7을 순차 구현한다. Story 본문은 사용자 지시에 따라 P1~P7의 전체 플레이 가능 판정 뒤 P8에서만 작성한다.

완료 증거는 `REVIVAL_ITEM`이 반영된 목록, 64/66/69 실행 파라미터 원장, 전수 그래프 감사, `DATA-REVISION-002`, `IMPLEMENTATION-HANDOFF-001`이 같은 후보 리비전을 가리키는 승인 기록이다. Story는 `CHAPTER_REVIEW + DEFERRED_UNTIL_PLAYABLE` 기록만 요구한다.

### P0 종료 기록 — 2026-08-24

- `PDG-0~6` 전부 통과
- 기획 입력 `s1-plan-20260824-r1`, 후보 레지스트리 `ws-content-r2.1-contract-r1`
- 계획 수량 `59/62/214/316/335/64/66/21/69/92/62/46/25`와 Day 50, Story 연결 ID 73+9 잠금
- 고아 참조·의도하지 않은 순환·75% 필수 수급 소프트락 0
- Story 본문 변경 0; P8 `CHAPTER_REVIEW` 유지

### P0.5 종료 기록 — 2026-08-24

- 기존 `ws-content-r2` 파일·기본 런타임 포인터 변경 0
- `ws-content-r2.1` 70파일 생성, manifest 해시 `c8f42722a19d4923ede77505d480ac3d5f279b983b6e5c180da18f04d839f511`
- 목표 수량 `62/316/335/64/66/69/92`와 명시 실행 필드 Java catalog 투영 완료
- manifest 68항목 해시, 43개 데이터 파일 schema, 전역 참조·필수 필드 L0 통과
- 동일 입력 2회 manifest 해시 일치, 기존 r2 회귀 포함 전체 110개 테스트 통과
- 후보 상태 `L0_VALIDATED`; 신규 일반 회차 기본 포인터 승격은 P9 전까지 금지

P1의 실제 증거 게이트는 2026-09-15 닫혔다. r2.1 후보는 계속 Test Lab·검증에서만 사용하고 기본 런타임 포인터는 r2로 유지한다. 다음 실행 단계는 `P2_READY`이며, P2 종료 증거가 생기기 전에는 Day 1~3 수직 구간을 완료로 표시하지 않는다.

## 3. 전체 확장 순서

각 단계는 작은 플레이 가능 수직 구간이다. 앞 단계의 정상·거부·저장 복구·실클라이언트 증거가 없으면 다음 Day 범위를 열지 않는다.

| 단계 | 플레이 범위 | 핵심 구현 | 종료 게이트 |
|---|---|---|---|
| `P0` 기획 완결 게이트 | 코드 없음 | `REVIVAL_ITEM`, 64/66/69 실행 파라미터·전수 그래프·ADR 잠금 | `PDG-0~6 PASS`, 후보 리비전·E2E 시나리오 승인 |
| `P0.5` 실행 데이터 투영 | Test Lab만 | 잠긴 원장을 생성기·schema·catalog로 기계 투영, 자연어 추론 제거 | L0 수량·참조·필수 필드·결정적 재생성 통과 |
| `P1` 기반 복구 | Test Lab·빈 회차 | r2 비용 레코드, 저장 마이그레이션, 개인/공용 원장 TX, AP 자극제·고정대·짧은 패링 상태 | 강제 종료·재접속·재시작에서 복제/소실 0 |
| `P2` Day 1~3 수직 구간 | 탐험→채집→craft 해금→장비→전투→연구 | 메뉴 GUI, 도감, 조합형 craft, 장착, 스탯, 기본 적·드롭·Day 전환 | 2~4인이 관리자 명령 없이 Day 3 종료 |
| `P3` Day 1~10 협동 알파 | 첫 보스까지 | 초반 목록 전수, 스킬/증강 최소 완성 풀, 상태·브레이크, Day 10 보스 | 1/2/3/4인 행렬과 BOSS-D10 복구 E2E |
| `P4` Day 11~20 정착 확장 | 시설·두 번째 보스 | 공용 물류고 전후 원장, 야영·정착 시설, 연구 D11~20, BOSS-D20 | 시설 파괴·이전·작업 복구와 두 번째 보스 통과 |
| `P5` Day 21~30 변이 확장 | 정화·균열·세 번째 보스 | 변이·오염 충돌, T4 자원, 장비·스킬·증강 확대, BOSS-D30 | 75% 수급에서도 호출·복구 소프트락 0 |
| `P6` Day 31~40 브레이크 확장 | 중단·공성·네 번째 보스 | 적 행동 69개 전체, 공성·시설 피해, 심연 장비 대가, BOSS-D40 | 전조·패링·브레이크·중단 대응 실플레이 판독 통과 |
| `P7` Day 41~50 재건/Final 기계 완성 | Story 개발 폴백을 포함한 전체 플레이 | 연구 D41~49, R01~R06, R03~R05 GUI, `REVIVAL_ITEM`, Final 상태·결말 원자 커밋 | Story 원문 없이도 Day 1~50을 정상 완주·실패·복구 가능 |
| `P8` Story 최종 작업 | P1~P7 플레이 가능 판정 뒤 | `CHAPTER_REVIEW`로 프롤로그→1~5장→Final 순차 작성·승인·payload 연결 | 장면 73·로그 9 원문, FULL/REDUCED 재생, 사용자 장별 승인 |
| `P9` RC·출시 | 전체 회차 반복 | 성능, 운영 명령, 리소스 팩, 접근성, 로컬라이징, canary | 전수 목록·TPS·실클라·복구·Story 증거와 `LIVE_LOCKED` 승인 |

## 4. 단계별 세부 작업

### P1 — 서버 권위 기반

1. `RunSnapshot`의 새 필드 마이그레이션 기본값과 구버전 회차 격리를 고정한다.
2. `RCOST/FCOST`를 실제 `validate→reserve→process→commit` 거래에 연결한다.
3. Lv1 시설은 제작식만 결제하고 설치 시 이중 차감하지 않는 테스트를 만든다.
4. 구조 고정대는 구조 대상·거리·AP가 유효한 첫 진행 틱에 예약을 소비하고 기본 +10%/강화 +25% 중 높은 하나만 적용한다.
5. F 입력은 패링 4틱과 단기 방어 12틱으로 연결하고 키 홀드·해제 의존을 제거한다.
6. AP 자극제의 5개 펄스가 재접속·정상 종료·강제 종료에서 중복되거나 사라지지 않는지 검증한다.

### P1 구현 기록 — 2026-08-24

- 회차 schema를 `3`으로 올리고 schema 1·2 마이그레이션, 개인 자원 원장, 비용 거래와 연구 active-time 필드를 기본값까지 정규화했다.
- `RCOST/FCOST`는 개인/공용 원장을 명시 선택한 뒤 `VALIDATED→RESERVED→PROCESSING→COMMITTED`로 저장하며, 아직 커밋되지 않은 `RESERVED/PROCESSING` 취소는 원래 원장에 정확히 반환한다. `CANCELLED` 거래는 같은 ID를 새 검증으로 재예약할 수 있고 시도 번호·취소 사유 이력을 보존하며, 재예약 차감 1회와 중복 클릭 차감 0회를 보장한다. 각 예약·처리·커밋·취소는 분리된 후보 스냅샷에서 실행하고 원자 저장 성공 뒤에만 권위 메모리로 채택한다. 콜백 또는 저장 실패 시 자원 차감·거래·연구·시설의 부분 변경은 현재 회차에 노출되지 않는다. 공용 결제는 활성 `FAC-S16` 없이는 거부한다.
- 시설 Lv1 설치는 제작식 참조만 사용하고 런타임 자원을 다시 차감하지 않는다. Lv2 이상만 정확한 FCOST 동가치 조합을 예약하며, 중단된 시설·연구 거래는 기동 복구에서 재개한다.
- 구조 고정대는 기본 `+10%`, 강화 `+25%` 중 높은 예약 하나만 보유하고, 거리·AP·구조 대상이 유효해 진행도가 실제 증가한 첫 틱에 활성화한다. 피격 시 아이템을 소비하거나 한 번의 중단을 무효화하지 않는다.
- 슬롯 `0` 일반 `F`는 보조 방패가 있을 때 `1틱 PARRY_STARTUP→4틱 PARRY_ACTIVE→12틱 SHORT_GUARD`로 전환된다. 패링 AP 15·성공 환급 5, 정면 150°/120°, 물리 70%·투사체 80%·마법/오염 40% 감소, 실행별 가드 충격과 방패 내구를 서버가 판정한다.
- AP 자극제는 테스트 배율을 반영한 AP 비용, 실제 아이템 수량 `-1`, 전투당 사용 횟수 `+1`, 즉시 `+10`, 이후 5개 `+3` 펄스 시작을 하나의 후보 회차에서 적용하고 원자 저장 성공 뒤에만 커밋한다. 각 후속 펄스도 분리 후보 스냅샷으로 체크포인트하며, 정상 종료는 남은 틱을 저장하고 강제 재기동은 마지막 성공 펄스부터 남은 횟수만 복원한다. Bukkit playerdata가 체크포인트보다 뒤처지면 재접속 시 회차 원장 수량으로 물리 아이템을 제거·복구해 복제와 소실을 막는다.
- 비용 실검증을 막던 Test Lab의 프로토타입 Day 제한 `1·3·6·10`을 제거했다. 명령과 GUI 모두 Day `1~50`을 지정·순환하며 `run.day`와 운영 `seasonDay`의 예산·사건 잠금을 함께 재생성한다. 따라서 시설 Lv2 게이트 Day 18과 모든 후반 Day를 동일 회차에서 재현할 수 있다.
- 자유 AI의 공격 타이밍으로 패링 증거가 흔들리지 않도록 `mob attack parry|guard|hit` 검증 경로를 추가했다. 바라보는 운영 적의 실제 첫 패턴을 각각 다음 F 뒤 2틱·8틱 또는 명령 뒤 20틱에 예약하며, 직접 피해 주입 없이 기존 `ENEMY_HIT_RESOLVED→ENEMY_ACTION_COMMITTED` 경로와 AI 복원을 사용한다.
- 전체 자동 검증: Gradle 48 suite, 168 test, failure/error/skip 0. schema 구버전 격리, 개인/공용 예약·취소·취소 후 재예약, `PROCESSING` 안전 환불과 연구·시설 복구 정리, 비용 단계 콜백·저장 실패의 후보 스냅샷 격리, 비용 축 중복 소비 금지, Lv1 이중 차감 금지, 고정대 높은 값/첫 진행 틱, F 상태 전이, 결정적 운영 적 공격 예약과 시간 정지 독립 서버 틱, 종료 회차 소유자 복구 권한 분리, undo 성공 후 스냅샷 소비, exit 정리·history 교체 실패 시 현재 회차 보존, AP 펄스·소비 수량 강제 reload fixture, 모든 생산 소모품 사용 횟수·AP 비용 원자 적용과 무효 범위 키 무변경, 수리 40%·시설 만료 연장, 정상 드롭·보관·획득 시 퀵 아이템 원장 기준선 이동, Windows 회차 파일 교체 재시도·버전 롤백을 포함한다. `check`에서 prototype 5파일·운영 r2 70파일·후보 r2.1 70파일도 함께 검증한다.
- Story 본문·장면·로그 payload 변경은 0이며 `CHAPTER_REVIEW`는 계속 P8로 유예한다.

P1은 코드·자동 증거와 아래 실제 증거를 모두 통과했다. 상태는 `P1_PASSED`, 다음 단계는 `P2_READY`다. 이는 P2 착수 허용일 뿐 Day 1~3 또는 Season 1 전체 완료 판정이 아니다.

| 실제 증거 | 판정 | 통과 근거 |
|---|---|---|
| `P1-CLIENT-F-001` | `PASS` | 바닐라 클라이언트에서 slot 0 방패 F, Shift+F 메뉴, slot 1~8 F 취소, 정면·후방, 패링·가드·붕괴와 장비/AP/내구 원장 일치를 확인 |
| `P1-CLIENT-COST-002` | `PASS` | 개인 연구와 FAC-S16 공용 시설 결제를 실행하고 실패 무차감·성공 단일 차감·거래·GUI를 재접속 전후 대조 |
| `P1-PROCESS-KILL-003` | `PASS` | AP 자극제 K0~K5와 비용 `RESERVED/PROCESSING` 각각에서 실제 JVM을 강제 종료하고 재시작 뒤 중복·유실 0을 확인 |

실제 클라이언트가 필요한 `P1-CLIENT-*`는 자동·콘솔 명령으로 대체하지 않는다. 기존 개발 서버의 회차·월드 데이터를 임의 기동해 변경하지 않고, 사용자와 격리 테스트 시간을 정한 뒤 별도 포트·저장소에서 수행한다.

2026-08-25 `non2error` 실클라이언트에서 Test Lab 입장, AP 충전, `EQL-OH-C01` 보조무기 장착을 확인했다. 이어 slot `1` 일반 `F`에서 서버 권위 `-106` 복구와 바닐라 손 교환이 충돌해 클라이언트 고스트 방패가 재현되었다. 엔티티 NBT와 회차 원장에는 동일 인스턴스 1개만 남았으므로 영속 복제는 아니었다.

동일 격리 서버에 모든 회차 내 바닐라 손 교환 취소 보정을 배포한 뒤 재검증했다. slot `1~8 + F`는 고정 보조무기 안내를 ActionBar에 표시하고 교환·복제·소실 없이 취소되었으며, 회차 원장은 동일한 `EQL-OH-C01` 인스턴스 1개를 유지했다. slot `0 + F`는 `SHORT_GUARD`, `Shift+F`는 `PLAYER_MENU`로 실제 클라이언트에서 각각 동작했다. 따라서 `P1-CLIENT-F-001`의 **입력 라우팅·고정 보조무기 무결성 하위 항목은 통과**했다. 정면·후방 공격과 패링·가드·가드 붕괴 피격 결과는 아직 미검증이므로 게이트 전체는 열어 둔다.

2026-09-15 `non2error` 실클라이언트에서 남은 피격 행렬을 닫았다. 정면 예약 공격은 `PARRIED`와 방패 내구 감소를, 서버가 시선을 정확히 180도 반전한 후 같은 실제 F 입력은 `PARRY_SUCCESS` 없이 `HIT`와 방패 내구 불변을 기록했다. 단기 방어는 시작 AP `100→85`, 가드 충격 `85→79`, 방패 `317→316`, `GUARDED`; AP 재생을 0으로 고정한 붕괴는 `GUARD_ONLY`, AP `1→0`, 방패 `316→315`, `broken=true`, `GUARDED`를 기록했다. 따라서 `P1-CLIENT-F-001`은 정면·후방·패링·가드·붕괴 및 기존 입력/고정 슬롯 무결성을 모두 통과했다.

2026-08-26 재입장 검증 중 Windows가 고정 임시 파일 `current.json.tmp`를 잠시 점유하여 `AccessDeniedException`이 3회 발생했다. 이후 저장이 회복되어 `test-9735d82c-dc6a-4914-bdff-02e43824ebf1` 회차는 `RUNNING`, version `175`, Day 2로 정상 종료 저장되었지만, 실패 구간의 상태 전이가 누락됐을 수 있으므로 해당 접속의 전투 결과는 P1 증거로 채택하지 않는다. 회차 저장을 저장별 고유 임시 파일, `AccessDenied` 한정 최대 5회(총 75ms) 재시도, 원자 이동 미지원 시 교체 이동, 최종 실패 시 메모리 version 롤백과 임시 파일 정리로 보강했다. 자동 회귀 통과 뒤 새 JAR로 같은 회차를 복원하여 실검증을 다시 시작한다.

같은 회차를 version `175→182`로 무오류 복원하고 `non2error` 재접속 뒤 야전 방패 원장·실제 왼손·인스턴스 일치를 확인했다. `EN-D1-01/ACT-EN-D1-01-PRIMARY` 정면 검증에서 실제 F 입력 뒤 `PARRY_SUCCESS→ENEMY_HIT_RESOLVED(outcome=PARRIED)→ENEMY_ACTION_COMMITTED`가 순서대로 기록되고 방패 내구가 `320→319`로 정확히 감소해 **정면 패링 하위 항목은 통과**했다. 이 과정에서 Test Lab 논리 시간이 정지되면 검증 공격의 `+2/+8틱`도 정지하는 결함이 드러났고, 시간을 재개하자 일반 AI까지 복귀해 회차가 `PARTY_WIPED` 되었다. 최신 사전 스냅샷으로 회차를 복구하고, 강제 검증 공격만 Bukkit 서버 틱을 사용해 시간 정지와 독립적으로 진행하며 실행 ID도 서버 틱으로 고유화하도록 보정했다. 후방·단기 방어·가드 붕괴는 보정 JAR 재배포 뒤 검증한다.

`PARTY_WIPED` 뒤 회차는 `ABORTED + restorePending`이었으나 기존 `undo`가 `RUNNING` 소유자만 허용해 최신 스냅샷 복구도 거부하는 사각지대가 확인됐다. 일반 Test Lab 변경 권한은 계속 `RUNNING` 소유자에게만 허용하고, `undo/exit` 복구 경로만 동일 UUID의 복구 대기 세션 소유자에게 허용하도록 분리했다. 다른 플레이어와 `restorePending=false` 종료 회차는 계속 거부하며, 성공 시 복원한 snapshot ID를 채팅에 표시한다. 현재 플레이 테스트 불가로 실배포 재검증은 보류하고 스냅샷·입장 전 백업은 유지한다.

플레이어 없는 Paper 콘솔 스모크에서 최종 P1 JAR `SHA-256 416315818D7F0015A90EB10231675695DEC182F4982780E80691CC1732849260`이 활성화되고 `ABORTED + restorePending` 회차를 인식했다. 기동 로그는 `/ws test undo` 최신 스냅샷 복원과 `/ws test exit RECOVERED --confirm` 입장 백업 복원을 함께 안내한다. 정상 종료 뒤 회차는 version `543`, 최신 snapshot `000019`는 `RUNNING/ACTIVE`, 백업은 유지됐고 저장 오류·임시 파일 잔류는 없었다.

추가 복구 감사에서 기존 undo가 최신 스냅샷을 먼저 삭제한 뒤 회차·플레이어를 복원하는 비원자 순서를 확인했다. 이제 최신 스냅샷은 비파괴로 읽고 회차 저장·플레이어 복원·감사 기록이 모두 성공한 뒤에만 정확한 run/snapshot ID로 삭제한다. 중간 실패 시 같은 복구 지점을 다시 사용할 수 있으며 다른 run ID를 통한 삭제는 거부한다. Test Lab 백업·스냅샷·프리셋·내보내기도 저장별 고유 임시 파일과 `AccessDenied` 제한 재시도를 공유한다. 보강 JAR `SHA-256 87FEB4BF47FA17CBF1F0E81F5F50FBC2E0527F08698D70F64E79ADD788AFF7D1`의 플레이어 없는 Paper 콘솔 스모크도 통과했다. 기동 시 같은 복구 대기 회차와 두 복구 명령을 안내했고, 정상 종료 뒤 회차 version `546`, 최신 snapshot `000019`, 입장 백업 1개가 유지됐다. 저장 오류와 `.tmp` 잔류는 0건이며 격리 서버는 종료 상태다.

같은 감사에서 `exit`가 입장 백업을 복원한 뒤 `restorePending=false`를 별도 저장하고 현재 회차를 제거해, 제거 단계의 파일 잠금 실패 시 복구 권한만 사라질 수 있음을 확인했다. 이제 입장 백업 복원과 감사 뒤에도 현재 파일은 복구 가능 상태로 유지하며, history 작성·임시 파일 정리·현재 파일 제거가 모두 성공한 경우에만 메모리 회차를 비우고 스냅샷·입장 백업을 소비한다. 현재 회차 제거 전 실패는 디스크와 메모리 모두 같은 소유자의 재시도를 허용하고, 제거 뒤 보조 파일 정리 실패는 완료를 되돌리지 않은 채 운영 경고와 잔류 파일로 보존한다.

exit 보강 JAR `SHA-256 FF2765858DBCDE72FE607BE3B73829328DB241D31D4EC80142366C4E9372694A`도 플레이어 없는 Paper 콘솔 스모크를 통과했다. 정상 종료 뒤 회차는 `ABORTED + restorePending=true`, version `549`, 최신 snapshot `000019(RUNNING/ACTIVE/timeFrozen=true)`, 입장 백업 1개를 유지했다. 저장 오류·임시 파일·열린 25567 포트는 모두 0이다.

회차 history도 최종 경로 직접 쓰기를 제거하고 저장별 고유 임시 파일과 원자 교체를 사용한다. history 교체 또는 기존 current 임시 파일 정리에 실패하면 권위 `current.json`을 삭제하지 않으며, 실패한 history 임시 파일은 정리한다. history 보강 JAR `SHA-256 2CC7935A1E102D602E1614947123D45302BF1A2D01C86AF5919E4A7514836941`의 콘솔 스모크 뒤에도 version `552`, 같은 복구 스냅샷·백업이 유지됐고 저장 오류·임시 파일·열린 포트는 0이었다.

자원 원장 감사에서는 환불된 `CANCELLED` 거래도 기존 `ALREADY_RESERVED`로 분류되어 같은 RCOST/FCOST 진행이 영구 봉쇄되는 소프트락을 확인했다. 이제 취소 거래만 새 `VALIDATED→RESERVED` 시도로 교체하며 `reservationAttempt`와 `cancellationReasons`를 보존한다. 재예약은 새 잔액에서 정확히 한 번 차감하고, 같은 시도의 중복 입력은 계속 차감 없이 거부한다. 재예약 보강 JAR `SHA-256 946D715886497B84852517BA61C8884E992E228BD0508CE179CCC90435864570`의 콘솔 스모크 뒤 회차는 version `555`, 복구 스냅샷 19개·입장 백업 1개를 유지했고 저장 오류·임시 파일·열린 포트는 0이었다.

후속 원자성 감사에서는 비용 예약이 권위 스냅샷의 잔액을 먼저 차감한 뒤 연구·시설 콜백을 실행해, 콜백 예외가 발생하면 다음 자동 저장에서 부분 변경이 굳어질 수 있는 경로를 확인했다. 이제 `RESERVED`, `PROCESSING`, `COMMITTED`, `CANCELLED` 전이는 모두 JSON 분리 복제본에서 도메인 변경까지 완료하고 `current.json` 원자 저장이 성공한 경우에만 `current`를 교체한다. 콜백 예외와 저장 예외에서 기존 version·잔액·거래·도메인 상태가 유지되는 4개 회귀 테스트를 추가했다. 전체 46 suite·155 test와 JAR `SHA-256 811F44773A3E21C31184A8D892B11F17249AD7B391B7F71AF3EB192C7EFCDC14`의 플레이어 없는 Paper 콘솔 스모크가 통과했다. 정상 종료 뒤 같은 복구 회차는 version `558`, 스냅샷 19개·입장 백업 1개를 유지했고 오류 로그·임시 파일·열린 25567 포트는 모두 0이다.

비용 복구 감사에서는 시설 작업 행이 없거나 손상된 `PROCESSING` 거래가 실패 사유만 기록한 채 자원을 영구 잠그고, 취소된 연구·시설의 도메인 행이 다음 재시도를 막거나 중복 작업으로 남을 수 있는 경로를 확인했다. `PROCESSING`은 커밋 콜백 적용 전 상태이므로 복구할 작업 근거가 없으면 예약 자원을 정확히 환불하고 `CANCELLED` 이력을 남긴다. 시설은 같은 transaction ID의 낡은 작업 행을 원자 취소와 함께 모두 제거하고, 연구는 잠긴 카탈로그에서 진행 시간·지불자·예약 비용을 복원하거나 존재하지 않는 target이면 환불하며, 취소된 노드는 가용성 판정을 다시 받는다. 전체 47 suite·160 test와 JAR `SHA-256 497B315E5AC3DF20239B46500C645D5C392C8D823EDA049D7A6CB9E0AD232F4D`의 콘솔 스모크가 통과했다. 회차 version은 `561`, 스냅샷 19개·입장 백업 1개가 유지됐고 오류 로그·임시 파일·열린 포트는 0이다.

AP·퀵 아이템 감사에서는 AP 자극제의 비용·물리 수량·효과·전투당 사용 횟수가 서로 다른 저장으로 확정되고, 정상적인 드롭·상자 보관·다른 플레이어의 획득을 재접속 복구와 같은 경로로 다뤄 아이템을 되살리거나 제거할 수 있는 경로를 확인했다. 이제 AP 자극제 최초 적용과 각 펄스는 저장 성공 뒤에만 활성 회차에 채택하고, 정상 인벤토리 전송은 다음 틱의 실제 수량을 새 개인 원장 기준선으로 저장한다. 반면 접속·회차 재로딩은 저장된 원장을 복구 권위로 사용한다. 전체 47 suite·162 test와 JAR `SHA-256 F8679B71892F8D11054DCF544C291148D66129A9E704CA669048CA05E463591F`의 콘솔 스모크가 통과했다. 회차 version은 `564`, 스냅샷 19개·입장 백업 1개가 유지됐고 자원 거래 0건, 오류 로그·임시 파일·열린 25567 포트는 모두 0이다. 실제 JVM 종료 경계와 1틱 전송 경계는 자동 증거만으로 통과 처리하지 않고 `P1-PROCESS-KILL-003`에 남긴다.

후속 소모품 감사에서는 AP 자극제 외 공용 소모품이 AP·아이템·사용 횟수를 분리 저장하고, 구조 고정대·수리 키트·휴대 정화기 충전이 아이템 차감 뒤 효과 원장을 별도 저장하며, 야전 배급팩이 채널 시작에 먼저 소비되어 접속 종료 시 사라지는 경로를 확인했다. 생산 소모품의 공용 스킬 비용과 사용 횟수, 직접 사용형 구조 고정대 예약·장비 내구 40% 복구·FAC-P05 만료 60초 연장을 각각 아이템 차감과 같은 후보 회차에 넣었다. 배급팩은 160/240틱 완료 시에만 소비하고 취소·접속/서버 종료 시 미소비한다. 데이터에만 존재하던 `CONTROL_CLEANSE`도 ROOT→SILENCE→DISARM 우선순위 실행에 연결했다. 전체 48 suite·168 test와 JAR `SHA-256 81F0B64BFDE887AF0F7612C58D2E84D922DBAD5AAEDD2DF4B53820DC03F22C02`의 플레이어 없는 Paper 콘솔 스모크가 통과했다. 종료 뒤 같은 회차는 `ABORTED + restorePending=true`, version `570`, 스냅샷 19개·입장 백업 1개·거래 0건을 유지했고 오류 로그·임시 파일·열린 25567 포트는 0이다. 정화 앰풀의 개인 오염 `-15`는 개인 오염 원장 자체가 아직 없어 구현하지 않았으며 해당 소모품 상태 원장에서 계속 `NOT_STARTED`로 추적한다.

원거리·F 비용 감사에서는 무기 공격의 AP, 원장 화살/석궁 탄창, 주무기 내구, 튜토리얼·개인 교전 범위가 별도 저장되고, 일반 화살 묶음 입금은 물리 아이템을 먼저 없앤 뒤 원장을 저장하며, 커스텀 화살 묶음이 바닐라 `ARROW` 폴백으로 계산될 수 있는 경로를 확인했다. 이제 일반 공격·무기 액티브·삼지창 전환은 서버 권위 비용을 한 후보 회차로 커밋하고, 등록 PDC가 없는 화살만 폴백으로 인정한다. 입금·폴백은 변경 후 절대 물리 수량을 먼저 체크포인트하고 Bukkit playerdata 저장 뒤 제거하여 재접속 복구를 멱등화했다. 순간 장전은 탄창 5~6발에서 AP·탄약 소비 없이 거부한다. F 시작 AP는 저장 성공 뒤에만 활성화하고, 패링 환급/가드 충격 AP와 방패 내구를 각각 같은 후보에 묶었다. 정책·schema 기본값을 포함한 전체 자동 검증은 49 suite·174 test, failure/error/skip 0으로 통과했다. 실제 클라이언트 입력과 JVM 종료 증거는 아직 채택하지 않았으므로 P1 상태는 `P1_IMPLEMENTED_AWAITING_CLIENT_EVIDENCE`를 유지한다.

동일 빌드 JAR `SHA-256 0C6735A0321953D6BAAAD3CB15D89C1906179F6FE9FC3BFF3FD007BA9CF939E6`을 25567 격리 서버에 배포했다. Paper 26.1.2-74에서 플러그인 활성화와 `ABORTED + restorePending` 회차 `test-9735d82c-dc6a-4914-bdff-02e43824ebf1` 인식을 확인했으며, 서버는 `non2error`의 `/ws test undo`와 남은 실클라이언트 증거 수집을 대기한다.

개인 비용 실검증 준비 중 Test Lab의 `research state ... READY`가 연구 GUI `open()` 재계산에서 `OBSERVABLE`로 강등되어 결제를 시작할 수 없는 결함을 재현했다. 가용성 재계산은 이제 `HIDDEN/OBSERVABLE/HYPOTHESIZED`에만 적용하고, 증거 소유 `READY`와 거래·완료 상태는 보존한다. 회귀 포함 49 suite·175 test와 전체 빌드가 통과했으며 보강 JAR `SHA-256 DACF14DB69CD4D0CDAB49F3A4F2AAAD1EF8D57690CF2CC3231203442AA99D7BB`를 같은 격리 서버에 배포했다. P1은 개인/공용 비용 재접속과 실제 JVM 종료 증거가 남아 있어 계속 `P1_IMPLEMENTED_AWAITING_CLIENT_EVIDENCE`다.

같은 서버 재접속 직전, 변화가 없는 정지 회차를 5초마다 동기 저장하던 heartbeat가 Windows 파일 쓰기에서 20초 이상 메인 스레드를 막는 워치독 증거를 포착했다. 명시적 거래 저장의 즉시 내구성은 유지하되, 자동 저장은 마지막 성공 JSON과 실제 상태가 다를 때만 version을 올리고 원자 교체하도록 보강했다. 회귀 포함 49 suite·176 test와 전체 빌드가 통과했고 JAR `SHA-256 1FA28201796CB00D33DC5CEED74E7EAEDA05095EF89CA538DA2D31C3E10458F8`를 재배포했다. 정지 회차 복구 후 3개 이상의 자동 저장 주기 동안 `version 1053`과 파일 수정 시각이 유지됐고 워치독·저장 예외가 0건임을 확인했다.

2026-09-15 `non2error` 실클라이언트에서 `P1-CLIENT-COST-002`를 닫았다. 개인 `RS-D01-SAMPLE`은 1인 환산 목재 `3→0`, 거래 `PROCESSING/PERSONAL/attempt 1`, 연구 `PROCESSING/0ms`가 일반 재접속 뒤에도 유지됐고 GUI도 시계 아이콘·`진행 0% · PROCESSING`을 표시했다. 공용 `FAC-S16`은 Day 18 Lv1 배치 뒤 목재 7·철 9·레드스톤 9·마력 결정 1을 공용 원장에서 정확히 한 번 차감하여 `Lv2/ACTIVE`, 거래 `COMMITTED/SHARED/attempt 1`, 작업 `COMPLETED`가 됐다. 좌클릭 개인 결제 실패는 어느 원장도 변경하지 않았고, 우클릭 공용 결제와 두 번째 일반 재접속 뒤 네 잔액 0·시설 HP `5060/5060`·GUI `Lv 2/5 · ACTIVE`가 일치했다. 같은 정확 비용 조합은 자동 회귀로 고정하며 P1 미결 증거는 `P1-PROCESS-KILL-003` 하나다.

같은 날 격리 포트 `25567`에서 `P1-PROCESS-KILL-003`을 닫았다. AP 자극제는 기준 AP 20에서 즉시 회복 직후 K0 `30/5회(version 1585, PID 35660)`, 펄스 직후 K1 `33/4회(1639, 11892)`, K2 `36/3회(1711, 27348)`, K3 `39/2회(1765, 14980)`, K4 `42/1회(1815, 37156)`, K5 `45/0회(1865, 21344)` 각각에서 종료 훅 없이 실제 JVM을 강제 종료했다. 매 재시작은 마지막 성공 체크포인트부터 남은 펄스만 적용해 AP 45·잔여 0회·퀵 아이템 0개·해당 전투 사용 1회로 수렴했고, `non2error` 재접속 후 엔티티 인벤토리도 매번 빈 배열이라 복제·잔존·유실이 없었다. 비용 `PROCESSING`은 `RS-D01-SAMPLE` version `1382→1383`, PID `10996` 강제 종료 뒤 목재 3 단일 예약과 거래·연구 상태가 유지됐다. 비용 `RESERVED`는 `RS-D03-SEPARATION`을 개인 목재 5·철 2, `QUEUED/RESERVED/PERSONAL/attempt 1`로 저장한 version `1929`, PID `34812`에서 강제 종료했고, 재시작 version `1931`에서 `PROCESSING`, 예약 수량 동일, 잔액 0, 시도 1회로 재개됐다. 재접속 뒤 연구 GUI도 `RS-D03-SEPARATION · 진행 0% · PROCESSING`을 표시했다. 이로써 P1 세 증거는 모두 `PASS`이며 P2를 열 수 있다.

### P2 — Day 1~3 첫 생산 수직 구간

1. 아무 원목 4개로 craft GUI를 해금한다.
2. 9칸 조합, 후보 순환, 재료 예약, 취소 반환, 출력 서명까지 한 거래로 만든다.
3. 최초 획득 도감 해금과 고정 도감 인덱스·`???` 표시를 연결한다.
4. Shift+F 메뉴에서 craft·도감·스탯·장비·설정을 연다.
5. slot 0/-106 장착, slot 1~8 바닐라, slot 0 전투 입력, 빈손 권투를 함께 E2E한다.
6. 최소 적·자원·장비·레시피·연구가 Day 3까지 하나의 루프로 이어지는지 2~4인으로 확인한다.

2026-09-15 P2 기반 구현에서 채집·보상 자원과 `PlayerState.personalResources`의 이중 권위를 제거했다. 신규 회차는 개인 자원 원장을 빈 권위값으로 시작하고, 구버전 회차만 최초 접속 때 실제 PDC 자원을 한 번 채택한다. 이후 획득·소비·연구/시설 개인 예약·취소·FAC-S16 입출금은 개인 원장을 먼저 원자 저장하고 `RESOURCE:<id>` 절대 수량 체크포인트로 Bukkit 인벤토리를 멱등 복구한다. 입출금은 개인 증감과 공용 증감을 한 후보 스냅샷에서 함께 커밋한다.

Craft 해금은 `craft-unlock:<runId>` 단일 커밋과 `VANILLA:ANY_LOG` 체크포인트로 원목 4개 소비를 묶었다. 3×3 결과 클릭은 `CRAFT` 거래에 입력 fingerprint, recipe/output ID·종류·수량, 출력 서명, 장비 instanceId와 내구 비율을 저장하고 `RESERVED→PROCESSING→COMMITTED`로 전이한다. 일반 아이템은 `REGISTERED:<id>` 목표 수량과 pending delivery, 자원 출력은 개인 원장과 `RESOURCE:<id>`, 장비는 고정 instanceId pending delivery로 물리 결과를 한 번만 만든다. GUI 정상 종료는 기존처럼 미소비 입력을 전부 반환하며, 저장소 아이템 설치도 시설 위치·원장 해금과 실물 소비를 같은 내구 경계로 묶었다. `CRAFT-UNLOCK` 시나리오는 잠금 상태와 slot 0을 침범하지 않는 원목 4개만, `CRAFT`는 거래 장애 주입용 해금·개인 자원을 준비한다. Test Lab은 이제 `RESERVED/PROCESSING/COMMITTED` 세 내구 경계를 각각 한 번 멈출 수 있고, 최신 제작 거래의 예약 자원·입력/출력 서명·instanceId를 인게임에서 검사한다. 순수 정책·저장 round-trip 회귀를 포함한 Gradle 자동 테스트는 51 suite·185 test, failure/error/skip 0이며 세 콘텐츠 번들도 통과했다. 배포 후보 JAR은 1,598,436 byte, SHA-256 `2C13E408A44C7217011F821276F16FF02DD77859AD373ACF54602643752020FE`다. 실제 클라이언트의 해금·배치·후보 순환·닫기 반환과 Craft 세 경계 JVM 종료 증거, 관리자 지급 없는 Day 1~3 루프는 아직 통과 처리하지 않는다.

### P3~P7 — 데이터 묶음 확장 규칙

- 새 Day 묶음을 열 때마다 그 구간의 재료, 비장비 아이템, 장비, 레시피, 스킬, 증강, 상태, 적 행동, 엔티티, 시설, 연구, loot를 함께 연다.
- 설명만 출력하는 placeholder는 등록 완료나 런타임 완료로 세지 않는다.
- 각 ID는 `HAPPY`, `REJECT`, `DUPLICATE`, `PERSIST`, `RECOVER`, `CLIENT` 여섯 증거 중 해당되는 항목을 가져야 한다.
- 파티 크기 1/2/3/4, Story/Challenge, Easy/Hard, TPS 20/15/10 행렬을 단계 종료마다 반복한다.
- 실제 클라이언트에서 블록 설치·채굴·섭취·인벤토리·숫자키가 검증되지 않은 단계는 Paper 스모크만으로 승격하지 않는다.

## 5. 전수 목록 완료 표

| 카테고리 | 고정 수 | 완료 판정 |
|---|---:|---|
| 재료 | 59 | 획득 원천·가공·도감·소비·회차 격리 |
| 비장비 아이템 | 62 | 직접/퀵 사용·거부·수량 경합·저장; `REVIVAL_ITEM` 포함 |
| 장비·도구 | 214 | 제작/드롭·장착·스탯·내구·파손·수리·재련 |
| 레시피 | 316 | 정확 배열·후보·예약·취소·출력·중복 방지; 부활 코어 1식 포함 |
| 스킬 | 64 | 입력·AP·쿨다운·operation·대상·상충 |
| 증강 | 66 | 드로우·등급 잠금·태그·진화·파티·몬스터 투영 |
| 상태 | 21 | 적용·저항·중첩·정화·보스·0피해·보호막 |
| 일반 적 행동 | 69 | 전조·판정·방어 등급·브레이크·상태·cleanup |
| 엔티티 | 92 | 생성·소유·수명·재시작·보상·제거 |
| loot | 62 | 기여·개인/파티 분배·천장·중복 수령 |
| 시설 | 46 | 설치·비용·작업·동력·피해·이전·파괴·복구 |
| 연구 | 25 | 증거·비용·대기열·중단·완료·해금 |
| 사건 | 107 | Day 선택·예산·성공/실패·보상·복구 |
| Story | 장면 73+로그 9 | 승인 원문·트리거·재생·선택 기록·결말 분리 |

한 행이라도 필요한 증거가 없으면 Season 1 전체를 완료로 표시하지 않는다.

## 6. 브랜치·커밋·후보 정책

- 현재 활성 목표의 모든 작업은 `S1_dev`에서 진행한다.
- 단계별로 데이터/코드/테스트를 분리 커밋하고, 사용자 소유 Gradle 캐시·보고서는 스테이징하지 않는다.
- 후보 서버는 기존 서버와 다른 디렉터리·포트·회차 저장소를 사용한다.
- `ws-content-r2.1-dev.N`은 신규 테스트 회차에만 적용하고 활성 r1/r2 회차의 리비전을 교체하지 않는다.
- P9 전에는 `ws-content-r2.1`을 `LIVE_LOCKED`로 부르지 않는다.

## 7. 진행·종료 규칙

현재 목표는 P0·P0.5 완료 뒤 P1~P7→P8→P9 순서로 계속한다. 각 단계는 앞 단계 종료 증거가 없으면 열지 않는다. P8 Story는 P1~P7 플레이 가능 판정 전에는 작성하지 않는다. P9에서 전체 요구사항을 증명해 목표를 완료한 뒤에는 다음 목표를 자동 실행하지 않고 사용자 요청을 기다린다.
