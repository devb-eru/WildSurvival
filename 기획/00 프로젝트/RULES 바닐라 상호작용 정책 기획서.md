# WildSurvival RULES 바닐라 상호작용 정책 기획서

## 문서 정보

| 항목 | 내용 |
|---|---|
| 문서 ID | `RULES-001` |
| 상태 | `BASELINE_LOCKED` |
| 문서 역할 | 바닐라 기능이 커스텀 진행·경제·하드코어 규칙을 우회하지 않도록 허용 범위를 관리 |
| 기본 전제 | 바닐라 야생 자유도는 유지하되 핵심 전투·성장 결과는 서버 플러그인이 확정 |
| 기본 UI | 리소스팩 없는 바닐라 클라이언트 기준 |
| 최종 수정일 | 2026-08-21 |

## 1. 정책 상태

| 상태 | 의미 |
|---|---|
| `ALLOW` | 바닐라 기능을 그대로 허용 |
| `ALLOW_CONTROLLED` | 허용하되 보상·속도·저장·악용 방지 규칙 적용 |
| `REPLACE` | 입력·블록은 사용할 수 있으나 결과는 커스텀 시스템으로 교체 |
| `LOCKED_DISCOVERY` | 발견형 진행 조건을 만족하기 전까지 차단 |
| `DISABLE` | 회차 내부에서 사용하지 않음 |

## 2. 전투와 생존

| 기능 | 기본 정책 | 처리 |
|---|---|---|
| 바닐라 체력·방어·공격 피해 | `REPLACE` | 커스텀 HP·ATK·DEF·피해 사용 |
| 바닐라 자연 회복 | `DISABLE` | 치료 시설, 소모품, 스킬로 회복 |
| 바닐라 음식 | `ALLOW_CONTROLLED` | 허기 회복은 허용, 커스텀 HP 직접 회복은 별도 데이터만 허용 |
| 바닐라 포션 전투 효과 | `REPLACE` | 시각 보조만 사용하고 실제 효과는 STATUS가 관리 |
| 불사의 토템 | `REPLACE` | 일반 부활을 차단하고 등록된 커스텀 부활 효과만 실행 |
| 바닐라 무적 틱 | `REPLACE` | 커스텀 타격 ID와 스킬 적중 간격 사용 |
| 낙하·용암·질식·익사·공허 | `REPLACE` | 바닐라 원인을 커스텀 환경 피해로 변환 |
| 아군 공격 | `DISABLE` | 기본 회차에서 피해·적대 상태·강제이동 차단 |
| 전술 아군 공격 규칙 | `ALLOW_CONTROLLED` | 회차 생성 선택 규칙, 비치명·보상 미발동 적용 |

## 3. 제작과 장비

| 기능 | 기본 정책 | 처리 |
|---|---|---|
| 일반 바닐라 제작 | `ALLOW_CONTROLLED` | 허용 목록의 생존·건축 아이템만 정상 제작 |
| 콘텐츠 장비 제작 | `REPLACE` | 커스텀 제작 GUI와 서버 레시피 사용 |
| 장비 강화·재련 | `REPLACE` | 설치형 시설 GUI에서 처리 |
| 인챈트 테이블 | `REPLACE` | 커스텀 인챈트 GUI 진입점 |
| 바닐라 인챈트 책 | `DISABLE` | 콘텐츠 장비에 적용 불가 |
| 모루 | `REPLACE` | 이름 변경 외 수리·인챈트 결합 차단, 필요 기능은 커스텀 GUI 제공 |
| 숫돌 | `REPLACE` | 콘텐츠 장비 옵션 제거·환급은 커스텀 정책 사용 |
| 바닐라 내구도 | `ALLOW_CONTROLLED` | 유틸리티와 콘텐츠 장비 정책을 분리하고 PDC 상태와 동기화 |
| 장비 직접 착용 | `REPLACE` | 콘텐츠 장비는 커스텀 장비 GUI로만 장착 |
| 주무기 슬롯 | `REPLACE` | 커스텀 GUI 장착 결과를 슬롯 `0`에 고정 동기화 |
| 보조무기 슬롯 | `REPLACE` | 커스텀 GUI 장착 결과를 슬롯 `-106`에 고정 동기화 |

## 4. 자원과 반복 생산

| 기능 | 기본 정책 | 처리 |
|---|---|---|
| 자연 광물·목재·작물 | `ALLOW_CONTROLLED` | 일반 재료 획득 허용, 핵심 진행 재료는 별도 보상 사건 필요 |
| 자동 농장 | `ALLOW_CONTROLLED` | 바닐라 산출물은 허용할 수 있으나 활동 EXP·핵심 재료·발견 진행을 반복 지급하지 않음 |
| 스포너·사육 개체 | `ALLOW_CONTROLLED` | 활동 EXP, 증강 발동 보상과 고유 드롭 차단 |
| 레드스톤 자동화 | `ALLOW_CONTROLLED` | 무인 서버 정지 중 콘텐츠 생산과 시설 작업은 진행하지 않음 |
| TNT 채굴 | `ALLOW_CONTROLLED` | 바닐라 블록 파괴는 월드 정책을 따르고 콘텐츠 자원 이벤트는 중복 지급하지 않음 |
| 어획·낚시 농장 | `ALLOW_CONTROLLED` | 반복 감쇠와 핵심 재료 획득 상한 적용 |

핵심 진행 재료는 생성 원인, 획득 이벤트 ID, 플레이어 또는 파티 수령 기록을 저장한다.

## 5. 거래와 보관

| 기능 | 기본 정책 | 처리 |
|---|---|---|
| 주민 거래 | `LOCKED_DISCOVERY` | 기본 차단, 추후 발견 노드가 허용한 거래군만 개방 가능 |
| 떠돌이 상인 | `ALLOW_CONTROLLED` | 일반 생존 물자만 제공, 핵심 진행·고유 장비 판매 금지 |
| 엔더 상자 | `ALLOW_CONTROLLED` | 회차별 별도 저장 또는 회차 외 아이템 반입·반출 차단 |
| 셜커 상자 | `LOCKED_DISCOVERY` | 휴대 보관 해금 전 사용 차단, 회차 종료 시 내용 저장 검증 |
| 아이템 액자·갑옷 거치대 | `ALLOW` | 전시와 건축 허용, 장비 효과는 장착 GUI 상태만 인정 |
| 호퍼 | `ALLOW_CONTROLLED` | 콘텐츠 시설·유품·고유 보상 컨테이너에는 접근 불가 |

## 6. 차원과 이동

| 기능 | 기본 정책 | 처리 |
|---|---|---|
| 침대 밤 건너뛰기 | `DISABLE` | Day 타이머와 월드 연출을 건너뛰지 못함 |
| 커스텀 Day 스킵 | `REPLACE` | `DAY-001`의 과반 투표와 정산 파이프라인만 허용, 침대와 분리 |
| 침대 스폰 설정 | `REPLACE` | 회차 안전 위치·야영 설비 기준 사용 |
| 네더 포탈 | `LOCKED_DISCOVERY` | CONTENT가 명시한 경우만 해금, 미해금 상태에서는 이동 차단 |
| 엔드 포탈 | `LOCKED_DISCOVERY` | 최종 진행 우회가 되지 않도록 기본 잠금 |
| 겉날개 | `LOCKED_DISCOVERY` | 이동·공세 회피에 미치는 영향 검증 후 콘텐츠 해금 가능 |
| 보트·광산 수레·말 | `ALLOW_CONTROLLED` | 일반 이동 허용, 전투·보스 영역별 제한 가능 |
| 명령어 순간이동 | `DISABLE` | 운영·복구 권한 외 회차 내 사용 금지 |
| 시설망 이동 | `REPLACE` | 발견·설치·동력·안전 조건을 만족한 커스텀 이동 사용 |

## 7. 월드 범위와 필수 자원 보장

- 핵심 진행은 특정 생물군계나 고정 구조물 하나에만 의존하지 않는다.
- 회차 월드는 운영 설정에 따라 사전 생성 범위와 월드 보더를 가질 수 있다.
- 보더 확장은 Day 또는 발견 진행과 연결할 수 있으나 임무 구역처럼 특정 좌표를 지시하지 않는다.
- 핵심 자원 탐색이 기준 시간을 넘으면 탐지 장치의 범주형 신호를 강화한다.
- 월드 시드에 필수 자원이 없거나 접근 불가능하면 대체 제작식 또는 보정 획득 경로를 활성화한다.
- 신규 청크 생성 자체가 무한 보상 전략이 되지 않도록 최초 발견 보상에 회차·플레이어별 상한을 둔다.

### 7.1 회차 시작 자원 검증

- 회차 시작 후보는 안전 발판, 2블록 높이 이동 경로, 임의 원목 4개, 노출 석재 8개, 식량 원천 1종이 중심 128블록 안에 있는지 검사한다.
- 조건을 만족하지 않으면 같은 월드 seed에서 최대 12개 후보를 다시 찾는다. 플레이어를 먼저 스폰한 뒤 재배치하지 않는다.
- 12개 후보가 모두 실패하면 중심 24~48블록의 지형을 훼손하지 않는 쓰러진 나무 표현으로 바닐라 원목 4개를 보장하고 해당 Manifest를 저장한다.
- 보장 원목은 Craft 해금에 쓸 수 있지만 별도 보상·EXP·도감·증강 발동을 만들지 않는다. 회차당 한 번만 생성한다.
- 특정 나무 종류·생물군계·구조물은 요구하지 않는다.

## 8. 커스텀 장비 입력 우선순위

### 좌클릭

```text
GUI·채팅·관전 여부 확인
→ 생명·제어 상태 확인
→ 장착 주무기 BASIC_ATTACK 또는 권투 가상 프로필 확인
→ 유효 전투 입력이면 서버 권위 실행 시작
→ 전투 입력이 아니면 허용 블록 파괴 처리
```

- 검·도끼·활·석궁·단검·둔기·지팡이·곡괭이·삼지창은 바닐라 기본 공격·피해를 사용하지 않는다. 슬롯 0 좌클릭을 장착 인스턴스에 연결된 `BASIC_ATTACK` 스킬로 변환한다.
- 장착 무기 기본 공격은 무기 액티브 장착 수에 포함하지 않는다. AP·간격·내구·탄약은 `SKILL-LIST-001 §2.1`의 10종 실행값을 사용한다.
- 권투는 아이템 귀속 기본 공격 스킬을 생성하지 않고 `UNARMED_COMBAT` 가상 콤보 프로필을 사용한다. 바닐라 피해는 취소하고 최종 피해는 공통 Combat 파이프라인이 확정한다.
- 곡괭이를 주무기로 장착한 상태에서는 전투 판정을 블록 파괴보다 우선한다.
- 곡괭이로 블록을 채굴하려면 같은 입력의 유효 공격 후보가 없고 블록 보호·도구 등급 검사를 통과해야 한다. 전투 상태만으로 채굴을 전부 차단하지 않는다.
- 주무기 슬롯이 비어 있으면 권투 상태를 활성화하므로 좌클릭은 권투 일반 공격을 우선한다.
- 권투 중 의도적인 블록 파괴는 웅크리기 유지 또는 별도 채굴 도구 사용 규칙으로 구분한다.

### 액티브·핫바·소모품

| 범위 | 입력·슬롯 | 역할 |
|---|---|---|
| 기본 공격 | slot 0 `L` | 현재 무기 또는 권투 BASIC |
| 무기 액티브 | slot 0 `R`, `Shift+L`, `Shift+R` | `W1~W3`, 최대 3개 |
| 보조무기 행동 | slot 0 `F` | 방패·보조무기, 실제 손 교환 취소 |
| 공용 액티브 | slot 0 `Shift+2~5` | `C1~C4`, 최대 4개 |
| 퀵 아이템 | slot 0 `Shift+6~9` | Q1~Q4 커스텀 소모품 바인딩 |
| 바닐라 자유 슬롯 | 일반 `2~9`, slot 1~8 L/R | 채굴·설치·섭취·아이템 사용 |

- 공용 액티브와 퀵 아이템은 입력 직전 선택 슬롯이 0일 때만 가로챈다. 성공·검증 실패·채널 취소 뒤 다음 틱에도 슬롯 0을 유지한다.
- 일반 `2~9`와 slot 1~8의 모든 L/R은 WS 스킬로 해석하지 않는다. 웅크린 채 블록을 설치하며 숫자 키를 쓰는 바닐라 플레이도 보존한다.
- `F`는 실제 손 교환을 취소하고 OFF 행동으로 변환하므로 주무기 0과 보조무기 -106이 바뀌지 않는다. `Shift+F`는 플레이어 메뉴를 연다.
- 복귀를 위해 서버가 발생시킨 held-slot 이벤트는 `inputSequence`로 무시해 재귀 발동을 차단한다.
- 유효한 빈사 구조·시설·등록 개체 상호작용은 충돌하는 액티브보다 우선하며 비용과 쿨다운을 중복 소비하지 않는다.

### 삼지창

- 바닐라 삼지창 투척, 충성, 급류, 찌르기 피해를 실제 전투 결과로 사용하지 않는다.
- 좌클릭 기본 공격과 `W1~W3` 액티브 입력을 커스텀 스킬 실행으로 변환한다.
- 투사체 위치, 관통, 회수와 소유권은 서버 데이터로 관리한다.
- 바닐라 삼지창 엔티티를 연출에 사용하더라도 실제 피해는 CustomDamageService만 확정한다.

## 9. UI 정책

- 핵심 기능은 인벤토리 GUI, 액션바, 보스바, 스코어보드, 타이틀과 채팅만으로 사용 가능해야 한다.
- 리소스팩 전용 아이콘은 대체 텍스트와 바닐라 아이템 표현을 제공한다.
- 장비 GUI를 닫거나 접속 종료해도 서버에 확정되지 않은 장착 변경은 적용하지 않는다.
- 여러 시스템이 액션바를 동시에 요청하면 생명 위험, 보스 기믹, AP·상태, 발견 알림 순으로 표시한다.

## 10. 검증 매트릭스

- 바닐라 기능별 이벤트 차단·변환·허용 여부
- 회차 외부 아이템 반입·반출
- PDC 제거·복제·아이템 스택 병합
- 자동 농장과 스포너 보상 차단
- 네더·엔드 미해금 우회
- 호퍼와 컨테이너를 이용한 고유 보상 복제
- 곡괭이 전투·채굴 입력 충돌
- 권투 상태와 빈 주무기 슬롯 동기화
- 삼지창 바닐라 피해와 커스텀 피해 중복
- 리소스팩 미사용 상태의 모든 핵심 UI 접근성
